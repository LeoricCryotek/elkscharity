# -*- coding: utf-8 -*-
# Non-model helpers.  Only imported explicitly by the code that needs
# them (see models/charity_contribution.py) to keep module startup fast
# and avoid importing `requests` when it isn't used.
